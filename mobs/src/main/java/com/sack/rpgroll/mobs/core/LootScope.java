package com.sack.rpgroll.mobs.core;

public enum LootScope {
    /** Se sortea una sola vez, entre todos los que participaron. */
    SHARED,
    /** Se sortea una vez POR CADA jugador que contribuyó daño. */
    PER_PLAYER
}

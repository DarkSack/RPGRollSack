# Idioma de los mensajes (comandos y GUIs de admin). Opciones: es, en, pt_BR.
# Los textos viven en lang/<idioma>.yml dentro de esta carpeta de datos —
# puedes editarlos ahí directamente. Tras cambiar esto, /npc reload.
language: es
